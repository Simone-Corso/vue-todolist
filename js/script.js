//creare la todo list, uguale come abbiamo fatto stamattina, facendo però due proprietà.

  const { createApp } = 'Vue';

  createApp({
    data() {
      return {
        
      }
    }
  }).mount('#app')
